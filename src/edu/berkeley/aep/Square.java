package edu.berkeley.aep;

// Understands a figure with four equal sides at right angles
public class Square extends Rectangle {
    public Square(int length) {
        super(length, length);
    }
}
